(ns rosso.core-test
  (:require [clojure.test :refer [are deftest is testing]]
            [rosso.core :refer [screen]]))

(deftest ^:unit a-test
  (testing "FIXME, I fail."
    (println "CALLED?")
    (is (= 1 1))))
